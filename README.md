# Katneshwarkar Legal Management

A legal case management dashboard built with React, TypeScript, Vite, and Supabase.

---

## 🔑 Project Accounts

| Service   | Account                  |
|-----------|--------------------------|
| Netlify   | dhanpegun                |
| Supabase  | rishi.sawant3005         |

---

## 🌐 Live Site

Hosted on Netlify — connected to this GitHub repo. Every push to `main` auto-deploys.

---

## 🛠 Local Setup

1. Clone the repo
2. Install dependencies:
   ```bash
   npm install
   ```
3. Create a `.env` file in the root (copy from `.env.example`):
   ```
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```
4. Run locally:
   ```bash
   npm run dev
   ```

---

## 🗄 Tech Stack

- **Frontend:** React + TypeScript + Vite
- **Styling:** Tailwind CSS + Framer Motion
- **Database:** Supabase (PostgreSQL)
- **Hosting:** Netlify
- **Auth:** Custom profile-based auth via Supabase
